function isError(value) {
  if (typeof value !== "object" || value === null || Array.isArray(value)) {
    return false;
  }
  const maybe = value;
  if (typeof maybe.name !== "string" || maybe.name === "") {
    return false;
  }
  if (typeof maybe.message !== "string") {
    return false;
  }
  return true;
}
function assertError(value) {
  if (!isError(value)) {
    throw new Error(`Encountered invalid error, got '${value}'`);
  }
}
function toError(value) {
  if (isError(value)) {
    return value;
  }
  if (typeof value === "string") {
    return new Error(value);
  }
  try {
    return new Error(`unknown error '${value}'`);
  } catch {
    return new Error(`unknown error of type '${typeof value}'`);
  }
}

export { assertError, isError, toError };
//# sourceMappingURL=assertion.esm.js.map
