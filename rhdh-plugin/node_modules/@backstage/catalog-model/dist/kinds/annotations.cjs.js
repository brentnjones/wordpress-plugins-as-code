'use strict';

var createCatalogModelLayer = require('../model/createCatalogModelLayer.cjs.js');
var constants = require('../entity/constants.cjs.js');
require('lodash');
require('ajv');
require('../validation/makeValidator.cjs.js');
var annotation = require('../location/annotation.cjs.js');

const wellKnownAnnotationsModel = createCatalogModelLayer.createCatalogModelLayer({
  layerId: "catalog.backstage.io/well-known-annotations",
  builder: (model) => {
    model.addAnnotation({
      name: annotation.ANNOTATION_LOCATION,
      description: "The location reference that the catalog uses to manage and update the entity."
    });
    model.addAnnotation({
      name: annotation.ANNOTATION_ORIGIN_LOCATION,
      description: "The original location reference that first discovered the entity."
    });
    model.addAnnotation({
      name: annotation.ANNOTATION_SOURCE_LOCATION,
      description: "The location reference of the source data for the entity, e.g. a file in a repository."
    });
    model.addAnnotation({
      name: "backstage.io/orphan",
      description: 'Set to "true" when the entity is not referenced by any location and is scheduled for deletion.'
    });
    model.addAnnotation({
      name: constants.ANNOTATION_VIEW_URL,
      description: "A URL to view the entity in an external tool, e.g. a source code repository."
    });
    model.addAnnotation({
      name: constants.ANNOTATION_EDIT_URL,
      description: "A URL to edit the entity in an external tool, e.g. a source code repository."
    });
  }
});

exports.wellKnownAnnotationsModel = wellKnownAnnotationsModel;
//# sourceMappingURL=annotations.cjs.js.map
