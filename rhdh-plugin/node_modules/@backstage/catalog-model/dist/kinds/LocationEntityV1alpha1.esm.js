import { createCatalogModelLayer } from '../model/createCatalogModelLayer.esm.js';
import jsonSchema from '../schema/kinds/Location.v1alpha1.schema.json.esm.js';
import { ajvCompiledJsonSchemaValidator } from './util.esm.js';

const locationEntityV1alpha1Validator = ajvCompiledJsonSchemaValidator(jsonSchema);
const locationEntityModel = createCatalogModelLayer({
  layerId: "catalog.backstage.io/kind-location",
  builder: (model) => {
    model.addKind({
      group: "backstage.io",
      names: {
        kind: "Location",
        singular: "location",
        plural: "locations"
      },
      description: "A Location is a marker that references other places to look for catalog data.",
      versions: [
        {
          name: ["v1alpha1", "v1beta1"],
          schema: {
            jsonSchema
          }
        }
      ]
    });
  }
});

export { locationEntityModel, locationEntityV1alpha1Validator };
//# sourceMappingURL=LocationEntityV1alpha1.esm.js.map
