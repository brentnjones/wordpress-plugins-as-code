import { createCatalogModelLayer } from '../model/createCatalogModelLayer.esm.js';
import { ANNOTATION_VIEW_URL, ANNOTATION_EDIT_URL } from '../entity/constants.esm.js';
import 'lodash';
import 'ajv';
import '../validation/makeValidator.esm.js';
import { ANNOTATION_LOCATION, ANNOTATION_ORIGIN_LOCATION, ANNOTATION_SOURCE_LOCATION } from '../location/annotation.esm.js';

const wellKnownAnnotationsModel = createCatalogModelLayer({
  layerId: "catalog.backstage.io/well-known-annotations",
  builder: (model) => {
    model.addAnnotation({
      name: ANNOTATION_LOCATION,
      description: "The location reference that the catalog uses to manage and update the entity."
    });
    model.addAnnotation({
      name: ANNOTATION_ORIGIN_LOCATION,
      description: "The original location reference that first discovered the entity."
    });
    model.addAnnotation({
      name: ANNOTATION_SOURCE_LOCATION,
      description: "The location reference of the source data for the entity, e.g. a file in a repository."
    });
    model.addAnnotation({
      name: "backstage.io/orphan",
      description: 'Set to "true" when the entity is not referenced by any location and is scheduled for deletion.'
    });
    model.addAnnotation({
      name: ANNOTATION_VIEW_URL,
      description: "A URL to view the entity in an external tool, e.g. a source code repository."
    });
    model.addAnnotation({
      name: ANNOTATION_EDIT_URL,
      description: "A URL to edit the entity in an external tool, e.g. a source code repository."
    });
  }
});

export { wellKnownAnnotationsModel };
//# sourceMappingURL=annotations.esm.js.map
