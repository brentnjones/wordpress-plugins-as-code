'use strict';

var util = require('./util.cjs.js');

function mergeJsonSchemas(target, source) {
  if ("type" in source && source.type !== target.type) {
    return { ...source };
  }
  const result = { ...target };
  for (const [key, value] of Object.entries(source)) {
    if (value === null) {
      delete result[key];
    } else if (util.isJsonObject(value) && util.isJsonObject(result[key])) {
      result[key] = mergeJsonSchemas(result[key], value);
    } else {
      result[key] = value;
    }
  }
  return result;
}

exports.mergeJsonSchemas = mergeJsonSchemas;
//# sourceMappingURL=mergeJsonSchemas.cjs.js.map
