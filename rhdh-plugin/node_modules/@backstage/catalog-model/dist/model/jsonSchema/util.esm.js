function isJsonObject(value) {
  return typeof value === "object" && value !== null && !Array.isArray(value);
}
function isJsonObjectDeep(value) {
  if (!isJsonObject(value)) {
    return false;
  }
  const seen = /* @__PURE__ */ new Set();
  return Object.values(value).every((v) => isJsonValueDeep(v, seen));
}
function isJsonValueDeep(value, seen) {
  if (value === null || typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
    return true;
  }
  if (typeof value !== "object") {
    return false;
  }
  if (seen.has(value)) {
    return false;
  }
  seen.add(value);
  if (Array.isArray(value)) {
    return value.every((v) => isJsonValueDeep(v, seen));
  }
  if (isJsonObject(value)) {
    return Object.values(value).every((v) => isJsonValueDeep(v, seen));
  }
  return false;
}

export { isJsonObject, isJsonObjectDeep };
//# sourceMappingURL=util.esm.js.map
