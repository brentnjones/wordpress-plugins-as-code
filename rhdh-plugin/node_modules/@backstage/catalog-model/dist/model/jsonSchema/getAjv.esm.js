import Ajv from 'ajv';
import ajvErrors from 'ajv-errors';

const getAjv = /* @__PURE__ */ (() => {
  let instance = void 0;
  return () => {
    if (!instance) {
      instance = new Ajv({
        allowUnionTypes: true,
        allErrors: true,
        validateSchema: true
      });
      ajvErrors(instance);
    }
    return instance;
  };
})();

export { getAjv };
//# sourceMappingURL=getAjv.esm.js.map
