import { z } from 'zod/v3';

const opRemoveAnnotationV1Schema = z.strictObject({
  op: z.literal("removeAnnotation.v1"),
  /**
   * The name of the annotation to remove, e.g. "backstage.io/techdocs-ref".
   */
  name: z.string()
});
function createRemoveAnnotationOp(input) {
  return opRemoveAnnotationV1Schema.parse({
    ...input,
    op: "removeAnnotation.v1"
  });
}

export { createRemoveAnnotationOp, opRemoveAnnotationV1Schema };
//# sourceMappingURL=removeAnnotation.esm.js.map
