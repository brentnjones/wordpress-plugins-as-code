import { z } from 'zod/v3';

const opRemoveKindV1Schema = z.strictObject({
  op: z.literal("removeKind.v1"),
  /**
   * The kind to remove, e.g. "Component".
   */
  kind: z.string()
});
function createRemoveKindOp(input) {
  return opRemoveKindV1Schema.parse({ ...input, op: "removeKind.v1" });
}

export { createRemoveKindOp, opRemoveKindV1Schema };
//# sourceMappingURL=removeKind.esm.js.map
