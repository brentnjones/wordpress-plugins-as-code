import { z } from 'zod/v3';

const opRemoveLabelV1Schema = z.strictObject({
  op: z.literal("removeLabel.v1"),
  /**
   * The name of the label to remove, e.g. "backstage.io/source-location".
   */
  name: z.string()
});
function createRemoveLabelOp(input) {
  return opRemoveLabelV1Schema.parse({ ...input, op: "removeLabel.v1" });
}

export { createRemoveLabelOp, opRemoveLabelV1Schema };
//# sourceMappingURL=removeLabel.esm.js.map
