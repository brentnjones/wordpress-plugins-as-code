class StaticCatalogModelSource {
  #layers;
  constructor(layers) {
    this.#layers = layers;
  }
  async *read(_options) {
    yield { data: this.#layers.map((layer) => ({ layer })) };
  }
}

export { StaticCatalogModelSource };
//# sourceMappingURL=StaticCatalogModelSource.esm.js.map
