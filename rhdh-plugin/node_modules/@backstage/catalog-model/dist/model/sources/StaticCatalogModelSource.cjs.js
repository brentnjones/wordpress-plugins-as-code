'use strict';

class StaticCatalogModelSource {
  #layers;
  constructor(layers) {
    this.#layers = layers;
  }
  async *read(_options) {
    yield { data: this.#layers.map((layer) => ({ layer })) };
  }
}

exports.StaticCatalogModelSource = StaticCatalogModelSource;
//# sourceMappingURL=StaticCatalogModelSource.cjs.js.map
