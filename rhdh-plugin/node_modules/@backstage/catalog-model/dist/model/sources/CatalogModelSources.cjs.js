'use strict';

var errors = require('@backstage/errors');
var defaultCatalogEntityModel = require('../defaultCatalogEntityModel.cjs.js');
var StaticCatalogModelSource = require('./StaticCatalogModelSource.cjs.js');
var uniqBy = require('lodash/uniqBy');

function _interopDefaultCompat (e) { return e && typeof e === 'object' && 'default' in e ? e : { default: e }; }

var uniqBy__default = /*#__PURE__*/_interopDefaultCompat(uniqBy);

class CatalogModelSources {
  /**
   * Provides the default catalog model.
   */
  static default() {
    return CatalogModelSources.static([]);
  }
  /**
   * Provides a static catalog model on top of the default one (which is
   * included automatically). User-provided layers take precedence over the
   * default model when layer IDs overlap.
   */
  static static(layers) {
    return new StaticCatalogModelSource.StaticCatalogModelSource(
      uniqBy__default.default([...layers, defaultCatalogEntityModel.defaultCatalogEntityModel], "layerId")
    );
  }
  constructor() {
    throw new errors.NotImplementedError();
  }
}

exports.CatalogModelSources = CatalogModelSources;
//# sourceMappingURL=CatalogModelSources.cjs.js.map
