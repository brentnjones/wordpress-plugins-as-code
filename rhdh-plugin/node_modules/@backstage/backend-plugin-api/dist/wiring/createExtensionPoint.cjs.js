'use strict';

function createExtensionPoint(options) {
  return {
    id: options.id,
    T: null,
    toString() {
      return `extensionPoint{${options.id}}`;
    },
    $$type: "@backstage/ExtensionPoint"
  };
}

exports.createExtensionPoint = createExtensionPoint;
//# sourceMappingURL=createExtensionPoint.cjs.js.map
