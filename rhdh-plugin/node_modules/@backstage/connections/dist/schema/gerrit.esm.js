import { createConnectionType } from '../system/createConnectionType.esm.js';
import { z } from 'zod/v4';

const GerritConnectionType = createConnectionType({
  type: "gerrit",
  title: "Gerrit",
  configSchema: z.object({
    host: z.string(),
    baseUrl: z.string().optional(),
    gitilesBaseUrl: z.string(),
    cloneUrl: z.string().optional()
  }),
  authMethods: [
    {
      method: "none",
      title: "None",
      configSchema: z.object({})
    },
    {
      method: "basic",
      title: "Basic",
      configSchema: z.object({
        username: z.string(),
        password: z.string()
      })
    }
  ]
});

export { GerritConnectionType };
//# sourceMappingURL=gerrit.esm.js.map
