import { createConnectionType } from '../system/createConnectionType.esm.js';
import { z } from 'zod/v4';

const GiteaConnectionType = createConnectionType({
  type: "gitea",
  title: "Gitea",
  configSchema: z.object({
    host: z.string(),
    baseUrl: z.string().optional()
  }),
  authMethods: [
    {
      method: "none",
      title: "None",
      configSchema: z.object({})
    },
    {
      method: "basic",
      title: "Basic",
      configSchema: z.object({
        username: z.string(),
        password: z.string()
      })
    }
  ]
});

export { GiteaConnectionType };
//# sourceMappingURL=gitea.esm.js.map
