import { createConnectionType } from '../system/createConnectionType.esm.js';
import { z } from 'zod/v4';

const GitlabConnectionType = createConnectionType({
  type: "gitlab",
  title: "GitLab",
  configSchema: z.object({
    host: z.string(),
    apiBaseUrl: z.string().optional(),
    baseUrl: z.string().optional()
  }),
  authMethods: [
    {
      method: "none",
      title: "None",
      configSchema: z.object({})
    },
    {
      method: "token",
      title: "Token",
      configSchema: z.object({
        token: z.string()
      })
    }
  ]
});

export { GitlabConnectionType };
//# sourceMappingURL=gitlab.esm.js.map
