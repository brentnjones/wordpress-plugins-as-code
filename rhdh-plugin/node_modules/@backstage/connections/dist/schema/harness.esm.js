import { createConnectionType } from '../system/createConnectionType.esm.js';
import { z } from 'zod/v4';

const HarnessConnectionType = createConnectionType({
  type: "harness",
  title: "Harness",
  configSchema: z.object({
    host: z.string()
  }),
  authMethods: [
    {
      method: "token",
      title: "Token",
      configSchema: z.object({
        token: z.string(),
        apiKey: z.string().optional()
      })
    }
  ]
});

export { HarnessConnectionType };
//# sourceMappingURL=harness.esm.js.map
