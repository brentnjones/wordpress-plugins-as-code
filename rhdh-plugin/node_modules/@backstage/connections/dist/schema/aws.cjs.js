'use strict';

var errors = require('@backstage/errors');
var createConnectionType = require('../system/createConnectionType.cjs.js');
var v4 = require('zod/v4');

function accountIdFromArn(arn) {
  const parts = arn.split(":");
  if (parts.length < 6 || parts[0] !== "arn") {
    throw new errors.InputError(`Invalid ARN "${arn}" in connection lookup`);
  }
  return parts[4] || void 0;
}
const AwsConnectionType = createConnectionType.createConnectionType({
  type: "aws",
  title: "AWS",
  cardinality: "singleton",
  lookupStrategy: "aws",
  configSchema: v4.z.object({
    roleName: v4.z.string().optional(),
    partition: v4.z.string().optional(),
    region: v4.z.string().optional(),
    externalId: v4.z.string().optional(),
    webIdentityTokenFile: v4.z.string().optional()
  }).refine((v) => !(v.partition && !v.roleName), {
    error: "partition requires a roleName to be provided"
  }).refine((v) => !(v.region && !v.roleName), {
    error: "region requires a roleName to be provided"
  }).refine((v) => !(v.externalId && !v.roleName), {
    error: "externalId requires a roleName to be provided"
  }).refine((v) => !(v.webIdentityTokenFile && !v.roleName), {
    error: "webIdentityTokenFile requires a roleName to be provided"
  }).refine((v) => !(v.webIdentityTokenFile && v.externalId), {
    error: "webIdentityTokenFile and externalId are mutually exclusive; AssumeRoleWithWebIdentity does not support external IDs"
  }),
  authMethods: [
    {
      method: "account",
      title: "Account",
      configSchema: v4.z.object({
        accountId: v4.z.string().optional(),
        mainAccount: v4.z.boolean().optional(),
        accessKeyId: v4.z.string().optional(),
        secretAccessKey: v4.z.string().optional(),
        profile: v4.z.string().optional(),
        roleName: v4.z.string().optional(),
        partition: v4.z.string().optional(),
        region: v4.z.string().optional(),
        externalId: v4.z.string().optional(),
        webIdentityTokenFile: v4.z.string().optional()
      }).refine((v) => v.accountId || v.mainAccount, {
        error: "accountId is required unless mainAccount is set"
      }).refine((v) => !(v.mainAccount && v.roleName), {
        error: "the mainAccount entry provides the credentials used to assume roles and cannot itself declare a roleName"
      }).refine((v) => !v.accessKeyId === !v.secretAccessKey, {
        error: "accessKeyId and secretAccessKey must be specified together, but only one was provided"
      }).refine((v) => !(v.profile && v.accessKeyId), {
        error: "profile and static credentials are mutually exclusive, but both were provided"
      }).refine((v) => !(v.profile && v.roleName), {
        error: "profile and roleName are mutually exclusive, but both were provided"
      }).refine((v) => !(v.externalId && !v.roleName), {
        error: "externalId requires a roleName to be provided"
      }).refine((v) => !(v.region && !v.roleName && !v.mainAccount), {
        error: "region requires a roleName to be provided"
      }).refine((v) => !(v.partition && !v.roleName), {
        error: "partition requires a roleName to be provided"
      }).refine((v) => !(v.webIdentityTokenFile && !v.roleName), {
        error: "webIdentityTokenFile requires a roleName to be provided"
      }).refine((v) => !(v.webIdentityTokenFile && v.accessKeyId), {
        error: "webIdentityTokenFile and static credentials are mutually exclusive, but both were provided"
      }).refine((v) => !(v.webIdentityTokenFile && v.profile), {
        error: "webIdentityTokenFile and profile are mutually exclusive, but both were provided"
      }).refine((v) => !(v.webIdentityTokenFile && v.externalId), {
        error: "webIdentityTokenFile and externalId are mutually exclusive; AssumeRoleWithWebIdentity does not support external IDs"
      })
    }
  ],
  matchAuth: (authMethods, query) => {
    const accountId = query.accountId ?? (query.arn ? accountIdFromArn(query.arn) : void 0);
    if (accountId) {
      const account = authMethods.find((a) => a.accountId === accountId);
      if (account) {
        return account;
      }
    }
    return authMethods.find((a) => a.mainAccount);
  },
  validate: ({ config, auth }) => {
    const seen = /* @__PURE__ */ new Set();
    for (const entry of auth) {
      if (entry.accountId) {
        if (seen.has(entry.accountId)) {
          throw new errors.InputError(
            `Multiple auth entries for AWS account "${entry.accountId}", but only one is allowed`
          );
        }
        seen.add(entry.accountId);
      }
    }
    const mainAccounts = auth.filter((a) => a.mainAccount);
    if (mainAccounts.length > 1) {
      throw new errors.InputError(
        "Multiple auth entries are marked as mainAccount, but only one is allowed"
      );
    }
    if (config.roleName && mainAccounts.length === 0) {
      throw new errors.InputError(
        "A connection-level roleName requires an auth entry marked as mainAccount to provide the credentials for assuming it"
      );
    }
  }
});

exports.AwsConnectionType = AwsConnectionType;
//# sourceMappingURL=aws.cjs.js.map
